

interface IArticleCardProps {
    title: string;
    content: string;
}

const ArticleCard = (props: IArticleCardProps) => {
    const { title, content } = props;
    return <div className="mt-[24px]">4545</div>
}

export default ArticleCard;